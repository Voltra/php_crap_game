{# Author : Ludwig GUERIN #}
{% extends "modules/layout.twig" %}

{% block keywords %}{% endblock %}

{% block title %}End{% endblock %}

{% if not error %}
    {% set error=[] %}
{% endif %}

{% block body %}
    {# TODO: create the view #}
    <h1 id="title">{% if won %}You won !{% else %}You lost !{% endif %}</h1>

    <div id="chart" data-pie='{{ chartData | raw }}' style="width: 80vw; height: 80vh;">
    </div>

    <h2>Win/Loss ratio: {{ (winRatio * 100) | number_format(2, '.', ',') }}%</h2>
{% endblock %}