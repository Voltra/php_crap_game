#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babili-inplace/bin/babili-inplace" "$@"
  ret=$?
else 
  node  "$basedir/../babili-inplace/bin/babili-inplace" "$@"
  ret=$?
fi
exit $ret
